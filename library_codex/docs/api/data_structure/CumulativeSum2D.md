<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.data_structure.CumulativeSum2D`

静的な二次元gridの任意の半開矩形和をO(1)で返す累積和。

- 計算量の目安: 各操作の計算量はAPI表を参照
- source: [`data_structure/CumulativeSum2D.py`](../../../data_structure/CumulativeSum2D.py)
- 公開API: function 0、class 1、method/property 1（Python protocol 0を含む）

## できること

- `CumulativeSum2D`: 静的な二次元gridの任意の半開矩形和をO(1)で返す累積和を扱う `CumulativeSum2D`。

## Import

```python
from library_codex.data_structure.CumulativeSum2D import CumulativeSum2D
```

## Class `CumulativeSum2D`

静的な二次元gridの任意の半開矩形和をO(1)で返す累積和を扱う `CumulativeSum2D`。

- constructor: [`CumulativeSum2D(matrix)`](../../../data_structure/CumulativeSum2D.py#L6)
- 引数: `matrix`: 行をlistで持つ行列
- 返り値: `CumulativeSum2D` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`sum(top, left, bottom, right)`](../../../data_structure/CumulativeSum2D.py#L25) | method | 半開区間 [left, right) の値を集約して返す。 | `top`: 矩形の上端（含む）<br>`left`: 半開区間の左端（含む）<br>`bottom`: 矩形の下端（含まない）<br>`right`: 半開区間の右端（含まない） | 区間・集合の和（入力要素型） |
