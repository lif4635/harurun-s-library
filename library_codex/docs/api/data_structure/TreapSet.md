<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.data_structure.TreapSet`

順序・k番目・前後要素を対数時間で扱う乱択平衡二分探索木。

- 計算量の目安: 各操作の計算量はAPI表を参照
- source: [`data_structure/TreapSet.py`](../../../data_structure/TreapSet.py)
- 公開API: function 0、class 1、method/property 17（Python protocol 5を含む）

## できること

- `TreapSet`: 順序・k番目・前後要素を対数時間で扱う乱択平衡二分探索木を扱う `TreapSet`。

## Import

```python
from library_codex.data_structure.TreapSet import TreapSet
```

## Class `TreapSet`

順序・k番目・前後要素を対数時間で扱う乱択平衡二分探索木を扱う `TreapSet`。

- constructor: [`TreapSet(values=())`](../../../data_structure/TreapSet.py#L10)
- 引数: `values`: 初期値のiterable。整数ならsizeを表す場合がある。省略時: `()`
- 返り値: `TreapSet` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`add(key)`](../../../data_structure/TreapSet.py#L84) | method | 引数で指定した要素・辺・区間へ値を追加する。 | `key`: 比較・格納に使うkey | bool |
| [`discard(key)`](../../../data_structure/TreapSet.py#L116) | method | 要素があれば削除する。 | `key`: 比較・格納に使うkey | bool |
| [`bisect_left(key)`](../../../data_structure/TreapSet.py#L141) | method | 条件を満たす最初の位置を二分探索する。 | `key`: 比較・格納に使うkey | 境界index（int） |
| [`bisect_right(key)`](../../../data_structure/TreapSet.py#L156) | method | 条件を満たす境界の直後を二分探索する。 | `key`: 比較・格納に使うkey | 境界index（int） |
| [`kth(index)`](../../../data_structure/TreapSet.py#L171) | method | 0-indexedでk番目の要素を取得する。 | `index`: 0-indexedの位置 | k番目の値 |
| [`ge(key, default=None)`](../../../data_structure/TreapSet.py#L186) | method | `ge`を求める。 | `key`: 比較・格納に使うkey<br>`default`: 省略時に使う値。省略時: `None` | `self.kth(index) if index < len(self) else default` |
| [`gt(key, default=None)`](../../../data_structure/TreapSet.py#L190) | method | `gt`を求める。 | `key`: 比較・格納に使うkey<br>`default`: 省略時に使う値。省略時: `None` | `self.kth(index) if index < len(self) else default` |
| [`le(key, default=None)`](../../../data_structure/TreapSet.py#L194) | method | `le`を求める。 | `key`: 比較・格納に使うkey<br>`default`: 省略時に使う値。省略時: `None` | `self.kth(index) if index >= 0 else default` |
| [`lt(key, default=None)`](../../../data_structure/TreapSet.py#L198) | method | `lt`を求める。 | `key`: 比較・格納に使うkey<br>`default`: 省略時に使う値。省略時: `None` | `self.kth(index) if index >= 0 else default` |
| [`min()`](../../../data_structure/TreapSet.py#L202) | method | 最小を求める。 | なし | `self.kth(0)` |
| [`max()`](../../../data_structure/TreapSet.py#L205) | method | 最大を求める。 | なし | `self.kth(len(self) - 1)` |
| [`__contains__(key)`](../../../data_structure/TreapSet.py#L208) | method | value in obj。 | `key`: 比較・格納に使うkey | bool |
| [`__len__()`](../../../data_structure/TreapSet.py#L211) | method | len(obj)。 | なし | 要素数（int） |
| [`__iter__()`](../../../data_structure/TreapSet.py#L215) | method | iter(obj)・for 文。 | なし | iterator[object] — 用途欄に示した要素を1つずつyieldする |
| [`tolist()`](../../../data_structure/TreapSet.py#L226) | method | 保持するkeyを昇順listで返す。O(N)。 | なし | list[object] — 保持するkeyを昇順に並べた列 |
| [`__str__()`](../../../data_structure/TreapSet.py#L230) | method | str(obj)・print(obj)で論理内容を表示する。 | なし | str instance |
| [`__repr__()`](../../../data_structure/TreapSet.py#L233) | method | 対話環境・debugger向けに型名付きで表示する。 | なし | 数値または入力要素型 `'TreapSet(%r)' % self.tolist()` |
