<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.data_structure.AdvancedRangeStructures`

Top-K区間集約・KD木・sortable sequence。

- 計算量の目安: 各構造の計算量
- source: [`data_structure/AdvancedRangeStructures.py`](../../../data_structure/AdvancedRangeStructures.py)
- 公開API: function 0、class 4、method/property 9（Python protocol 0を含む）

## Import

```python
from library_codex.data_structure.AdvancedRangeStructures import TopKNode, RangeAddCountTopK, LazyKDTree, SortableSegmentTree
```

## Class `TopKNode`

Top-K区間集約・KD木・sortable sequenceを扱う `TopKNode`。

- constructor: [`TopKNode(x: object, f: object)`](../../../data_structure/AdvancedRangeStructures.py#L6)
- 引数: `x`: 値・座標・問い合わせ対象<br>`f`: `f`として渡す値（APIの文脈に従う）
- 返り値: `TopKNode` instance

## Class `RangeAddCountTopK`

Top-K区間集約・KD木・sortable sequenceを扱う `RangeAddCountTopK`。

- constructor: [`RangeAddCountTopK(length, k, fill_value=0, fill_frequency=1, minimum=None, zero=0)`](../../../data_structure/AdvancedRangeStructures.py#L14)
- 引数: `length`: 長さ<br>`k`: 個数・順位・移動量（APIの文脈に従う）<br>`fill_value`: `fill`・値として渡す値（APIの文脈に従う）。省略時: `0`<br>`fill_frequency`: `fill`・頻度として渡す値（APIの文脈に従う）。省略時: `1`<br>`minimum`: 最小として渡す値（APIの文脈に従う）。省略時: `None`<br>`zero`: 加法単位元・0相当の値。省略時: `0`
- 返り値: `RangeAddCountTopK` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`range_add(left, right, delta)`](../../../data_structure/AdvancedRangeStructures.py#L67) | method | 区間・`add`を処理する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`delta`: 加算差分 | `None` |
| [`range_top_k(left, right)`](../../../data_structure/AdvancedRangeStructures.py#L91) | method | `RangeAddCountTopK` の `range_top_k method` を実行する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない） | 計算結果（list） |
| [`top_k()`](../../../data_structure/AdvancedRangeStructures.py#L114) | method | `RangeAddCountTopK` の `top_k method` を実行する。 | なし | 計算結果 |
| [`rangeAdd`](../../../data_structure/AdvancedRangeStructures.py#L89) | alias | `range_add` の別名。 | 同じ | 同じ |
| [`rangeTopK`](../../../data_structure/AdvancedRangeStructures.py#L112) | alias | `range_top_k` の別名。 | 同じ | 同じ |
| [`topK`](../../../data_structure/AdvancedRangeStructures.py#L121) | alias | `top_k` の別名。 | 同じ | 同じ |

## Class `LazyKDTree`

Top-K区間集約・KD木・sortable sequenceを扱う `LazyKDTree`。

- constructor: [`LazyKDTree(xs, ys, weights, combine=lambda a, b: a + b, identity=0, mapping=lambda value, action, size: value + action * size, composition=lambda old, new: old + new, lazy_identity=0)`](../../../data_structure/AdvancedRangeStructures.py#L131)
- 引数: `xs`: `xs`として渡す値（APIの文脈に従う）<br>`ys`: `ys`として渡す値（APIの文脈に従う）<br>`weights`: 重みの列<br>`combine`: `combine`として渡す値（APIの文脈に従う）。省略時: `lambda a, b: a + b`<br>`identity`: 演算 `op` の単位元。省略時: `0`<br>`mapping`: 作用を値へ適用するcallback。省略時: `lambda value, action, size: value + action * size`<br>`composition`: 新旧の作用を合成するcallback。省略時: `lambda old, new: old + new`<br>`lazy_identity`: `lazy`・`identity`として渡す値（APIの文脈に従う）。省略時: `0`
- 返り値: `LazyKDTree` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`update(left, right, down, up, action)`](../../../data_structure/AdvancedRangeStructures.py#L248) | method | 指定位置・辺・状態を更新する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`down`: `down`として渡す値（APIの文脈に従う）<br>`up`: `up`として渡す値（APIの文脈に従う）<br>`action`: 遅延作用・更新作用 | `None` |
| [`set(index, value)`](../../../data_structure/AdvancedRangeStructures.py#L274) | method | 指定位置・状態を値で置き換える。 | `index`: 0-indexedの位置<br>`value`: 追加・設定・問い合わせる値 | `None` |
| [`query(left, right, down, up)`](../../../data_structure/AdvancedRangeStructures.py#L289) | method | 指定した対象への問い合わせ結果を返す。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`down`: `down`として渡す値（APIの文脈に従う）<br>`up`: `up`として渡す値（APIの文脈に従う） | 問い合わせ結果（型・tuple形状はclassの用途に従う） |

## Class `SortableSegmentTree`

Sortable sequence with point update and monoid range product.

- constructor: [`SortableSegmentTree(keys, values, op=lambda a, b: a + b, identity=0, block_size=None)`](../../../data_structure/AdvancedRangeStructures.py#L317)
- 引数: `keys`: 整列keyの列またはkey callback<br>`values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`op`: 結合的な二項演算 `op(left, right)`。省略時: `lambda a, b: a + b`<br>`identity`: 演算 `op` の単位元。省略時: `0`<br>`block_size`: `block`・`size`として渡す値（APIの文脈に従う）。省略時: `None`
- 返り値: `SortableSegmentTree` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`update(index, key, value)`](../../../data_structure/AdvancedRangeStructures.py#L344) | method | 指定位置・辺・状態を更新する。 | `index`: 0-indexedの位置<br>`key`: 比較・格納に使うkey<br>`value`: 追加・設定・問い合わせる値 | `None` |
| [`query(left, right)`](../../../data_structure/AdvancedRangeStructures.py#L351) | method | 指定した対象への問い合わせ結果を返す。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない） | 問い合わせ結果（型・tuple形状はclassの用途に従う） |
| [`sort(left, right, reverse=False)`](../../../data_structure/AdvancedRangeStructures.py#L364) | method | `SortableSegmentTree` の `sort method` を実行する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`reverse`: 逆向き・降順を使うかどうか。省略時: `False` | `None` |
| [`set`](../../../data_structure/AdvancedRangeStructures.py#L349) | alias | `update` の別名。 | 同じ | 同じ |
