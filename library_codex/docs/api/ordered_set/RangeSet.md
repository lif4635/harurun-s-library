<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.ordered_set.RangeSet`

互いに素な整数区間を追加・削除し、包含判定とmexを行う集合。

- 計算量の目安: 各操作の計算量はAPI表を参照
- source: [`ordered_set/RangeSet.py`](../../../ordered_set/RangeSet.py)
- 公開API: function 0、class 1、method/property 6（Python protocol 1を含む）

## できること

- `RangeSet`: 互いに素な整数区間を追加・削除し、包含判定とmexを行う集合を扱う `RangeSet`。

## Import

```python
from library_codex.ordered_set.RangeSet import RangeSet
```

## Class `RangeSet`

互いに素な整数区間を追加・削除し、包含判定とmexを行う集合を扱う `RangeSet`。

- constructor: [`RangeSet()`](../../../ordered_set/RangeSet.py#L8)
- 引数: なし
- 返り値: `RangeSet` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`add(left, right)`](../../../ordered_set/RangeSet.py#L13) | method | 引数で指定した要素・辺・区間へ値を追加する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない） | `0` / `added`（数値または入力要素型） |
| [`discard(left, right)`](../../../ordered_set/RangeSet.py#L36) | method | 要素があれば削除する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない） | `0` / `removed`（int） |
| [`contains(value)`](../../../ordered_set/RangeSet.py#L65) | method | 指定値を保持しているか判定する。 | `value`: 追加・設定・問い合わせる値 | bool |
| [`mex(value=0)`](../../../ordered_set/RangeSet.py#L69) | method | `mex`を求める。 | `value`: 追加・設定・問い合わせる値。省略時: `0` | `self.ends[start]` / 値 |
| [`intervals()`](../../../ordered_set/RangeSet.py#L75) | method | 現在保持している互いに素な半開区間を始点順に列挙する。 | なし | list[tuple[int, int]] — 保持中の半開区間 $[\mathrm{left},\mathrm{right})$ を始点順に並べた列。 |
| [`__len__()`](../../../ordered_set/RangeSet.py#L78) | method | len(obj)。 | なし | 要素数（int） |
