<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.optimization.AliensTrick`

個数penalty付き最適化から個数固定の最適値を復元する。

- source: [`optimization/AliensTrick.py`](../../../optimization/AliensTrick.py)
- 公開API: function 1、class 0、method/property 0（Python protocol 0を含む）

## できること

- 選択個数に整数penaltyを加えた最適化oracleから、選択個数を固定した最適値を復元できる。
- penalty範囲を指数探索してから二分探索し、最小化と最大化の両方を扱う。
- count=targetの解がLagrangian relaxationで復元できる離散凸・離散凹な問題を対象にする。

## Import

```python
from library_codex.optimization.AliensTrick import aliens_trick
```

## Functions

| signature | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- |
| [`aliens_trick(target, solve, minimize=True, max_abs_penalty=1 << 60)`](../../../optimization/AliensTrick.py#L48) | base_score + penalty * countの最適値を返すoracleを整数penaltyで呼び、countをtargetへ固定したbase_scoreを復元する。 | `target`: 固定したい非負の選択個数。<br>`solve`: solve(penalty)が(penalized_score, count)を返すcallback。同値解では、minimize=Trueならpenalty増加に対してcountが非増加、Falseなら非減少になるよう一貫したtie処理をcallback側で行う。<br>`minimize`: Trueなら最小化、Falseなら最大化oracleとして扱う。省略時: `True`<br>`max_abs_penalty`: bracket探索で許すpenalty絶対値の上限。targetを挟めなければValueError。省略時: `1 << 60` | tuple[number, int] — (base_score, penalty)を返す。 | O(log P) 回のsolve呼び出し、O(1) extra memory（Pは探索したpenalty幅） |
