<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.polynomial.PolynomialDivision998`

Fast polynomial quotient and remainder modulo 998244353.。

- source: [`polynomial/PolynomialDivision998.py`](../../../polynomial/PolynomialDivision998.py)
- 公開API: function 3、class 0、method/property 0（Python protocol 0を含む）

## できること

- `poly_div`: 998244353上で2多項式を割り、商を返す。
- `poly_divmod`: 998244353上で2多項式を割り、商と余りを返す。
- `poly_mod`: 998244353上で2多項式を割り、余りだけを返す。

## Import

```python
from library_codex.polynomial.PolynomialDivision998 import poly_div, poly_divmod, poly_mod
```

## Functions

| signature | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- |
| [`poly_div(dividend, divisor)`](../../../polynomial/PolynomialDivision998.py#L7) | 998244353上で2多項式を割り、商を返す。 | `dividend`: 被除多項式の昇べき順係数列。<br>`divisor`: 0でない除多項式の昇べき順係数列。 | list[int] — 998244353上の多項式除算の商を、最高次側の0を除いた昇べき順係数列で返す。 | O(N log N) |
| [`poly_divmod(dividend, divisor)`](../../../polynomial/PolynomialDivision998.py#L40) | 998244353上で2多項式を割り、商と余りを返す。 | `dividend`: 被除多項式の昇べき順係数列。<br>`divisor`: 0でない除多項式の昇べき順係数列。 | (quotient, remainder) — 998244353上の多項式除算の商と余り。余りの次数は除数より小さい。 | O(N log N) |
| [`poly_mod(dividend, divisor)`](../../../polynomial/PolynomialDivision998.py#L61) | 998244353上で2多項式を割り、余りだけを返す。 | `dividend`: 被除多項式の昇べき順係数列。<br>`divisor`: 0でない除多項式の昇べき順係数列。 | list[int] — 998244353上の多項式除算の余りを、最高次側の0を除いた昇べき順係数列で返す。 | O(N log N) |
