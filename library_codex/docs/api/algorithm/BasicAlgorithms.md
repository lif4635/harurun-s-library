<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.algorithm.BasicAlgorithms`

Fibonacci・転倒数・LIS・0/1 knapsack・subset sum・Mo・doubling。

- 計算量の目安: 各標準計算量
- source: [`algorithm/BasicAlgorithms.py`](../../../algorithm/BasicAlgorithms.py)
- 公開API: function 24、class 2、method/property 5（Python protocol 0を含む）

## できること

- Fibonacci数・転倒数・LIS・0/1 knapsackなど、頻出する列アルゴリズムを使える。
- Mo's algorithm、ダブリング、座標圧縮、区間併合、二分探索を1ファイルから選べる。
- radix sort・quickselect・部分集合列挙など、Python標準処理では遅くなりやすい部分を置き換えられる。

## Import

```python
from library_codex.algorithm.BasicAlgorithms import (
    fibonacci,
    inversion_count,
    longest_increasing_subsequence,
    knapsack_01,
    knapsack_01_max,
    subset_sum_possible,
    subset_sum_restore,
    coordinate_compress,
    merge_intervals,
    binary_search_int,
    binary_search_float,
    bit_indices,
    submasks,
    supermasks,
    kth_element,
    radix_sort_nonnegative,
    popcount,
    most_significant_bit_index,
    least_significant_bit_index,
    ensure_permutation,
    permute,
    permute_in_place,
    bucket_sort_permutation,
    bucket_sort,
    Mo,
    Doubling,
)
```

## Functions

| signature | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- |
| [`fibonacci(index, mod=None)`](../../../algorithm/BasicAlgorithms.py#L6) | index番目のFibonacci数を高速doublingで求める。 | `index`: 0-indexedの位置<br>`mod`: 法。Noneの場合は整数上の演算。省略時: `None` | `first` |
| [`inversion_count(values)`](../../../algorithm/BasicAlgorithms.py#L26) | 列の転倒数と座標圧縮した順位列を求める。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある | 答え（int） |
| [`longest_increasing_subsequence(values, strict=True, restore=False)`](../../../algorithm/BasicAlgorithms.py#L46) | `longest`・`increasing`・部分列を求める。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`strict`: 狭義（等値を許さない）として扱うか。省略時: `True`<br>`restore`: 復元情報も計算するか。省略時: `False` | `len(tails)` / tuple(`len(tails)`, 位置のlist, list `[values[index] for index in indices]`) |
| [`knapsack_01(weights, values, capacity)`](../../../algorithm/BasicAlgorithms.py#L74) | 各品物を高々1回選ぶ0/1 knapsackの到達可能状態を計算する。 | `weights`: 重みの列<br>`values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`capacity`: 容量 | `dp`（数値または入力要素型） |
| [`knapsack_01_max(weights, values, capacity)`](../../../algorithm/BasicAlgorithms.py#L93) | 各品物を高々1回選ぶ0/1 knapsackの最大価値を求める。 | `weights`: 重みの列<br>`values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`capacity`: 容量 | `max((value for value in knapsack_01(weights, values, capacity)...` |
| [`subset_sum_possible(values, limit=None)`](../../../algorithm/BasicAlgorithms.py#L98) | 部分集合・和・`possible`を計算する。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`limit`: 上限。NoneならAPI既定の上限。省略時: `None` | `bits`（int） |
| [`subset_sum_restore(values, target)`](../../../algorithm/BasicAlgorithms.py#L111) | 部分集合・和・`restore`を計算する。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`target`: 探索・判定・更新の対象値 | list[object] — 計算結果 / `None` |
| [`coordinate_compress(values)`](../../../algorithm/BasicAlgorithms.py#L226) | 値の大小関係を保った0始まりの順位へ座標圧縮する。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある | tuple(`ordered`, `mapping`（dict）) |
| [`merge_intervals(intervals, merge_adjacent=True)`](../../../algorithm/BasicAlgorithms.py#L232) | 重なる区間を併合し、互いに交わらない半開区間列を返す。 | `intervals`: 区間 `(left, right)` のiterable<br>`merge_adjacent`: 処理中に呼び出す関数または操作。省略時: `True` | list[object] — 用途欄に示した結果を1要素ずつ並べた列 |
| [`binary_search_int(predicate, false_value, true_value)`](../../../algorithm/BasicAlgorithms.py#L248) | 判定条件が切り替わる整数境界を二分探索する。 | `predicate`: 判定callback<br>`false_value`: 処理対象の値<br>`true_value`: 処理対象の値 | `true_value`（数値または入力要素型） |
| [`binary_search_float(predicate, false_value, true_value, iterations=80)`](../../../algorithm/BasicAlgorithms.py#L259) | 判定条件が切り替わる実数境界を指定回数の二分探索で近似する。 | `predicate`: 判定callback<br>`false_value`: 処理対象の値<br>`true_value`: 処理対象の値<br>`iterations`: 反復回数。省略時: `80` | `true_value`（数値または入力要素型） |
| [`bit_indices(mask)`](../../../algorithm/BasicAlgorithms.py#L269) | 整数の2進表現で1になっているbit位置を昇順に列挙する。 | `mask`: bit mask | iterator[object] — 用途欄に示した要素を1つずつyieldする |
| [`submasks(mask, include_zero=True)`](../../../algorithm/BasicAlgorithms.py#L276) | maskの部分maskを大きい順に列挙する。 | `mask`: bit mask<br>`include_zero`: 0も列挙結果に含めるか。省略時: `True` | iterator[object] — 用途欄に示した要素を1つずつyieldする |
| [`supermasks(mask, bit_count)`](../../../algorithm/BasicAlgorithms.py#L285) | 指定bit幅の範囲でmaskを含む上位maskを列挙する。 | `mask`: bit mask<br>`bit_count`: 使用bit数 | iterator[object] — 用途欄に示した要素を1つずつyieldする |
| [`kth_element(values, index)`](../../../algorithm/BasicAlgorithms.py#L295) | 列を部分的に並べ替え、0始まりでk番目に小さい値を返す。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`index`: 0-indexedの位置 | `pivot` / `values[left]` |
| [`radix_sort_nonnegative(values, bits=64, digit_bits=11)`](../../../algorithm/BasicAlgorithms.py#L326) | 非負整数列をradix sortで昇順に並べる。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`bits`: 使用bit数。省略時: `64`<br>`digit_bits`: radix 1桁に使うbit数。省略時: `11` | list[int] — 頂点または要素の位置を結果順に並べた列 |
| [`popcount(value)`](../../../algorithm/BasicAlgorithms.py#L349) | `popcount`を求める。 | `value`: 追加・設定・問い合わせる値 | `value.bit_count()` |
| [`most_significant_bit_index(value)`](../../../algorithm/BasicAlgorithms.py#L355) | `most`・`significant`・`bit`・`index`を求める。 | `value`: 追加・設定・問い合わせる値 | 数値または入力要素型 `value.bit_length() - 1` |
| [`least_significant_bit_index(value)`](../../../algorithm/BasicAlgorithms.py#L361) | `least`・`significant`・`bit`・`index`を求める。 | `value`: 追加・設定・問い合わせる値 | 数値または入力要素型 `(value & -value).bit_length() - 1` |
| [`ensure_permutation(permutation)`](../../../algorithm/BasicAlgorithms.py#L367) | 列が0からn-1までを1回ずつ含む置換か判定する。 | `permutation`: 0-indexed置換 `p[i]` の列 | bool |
| [`permute(values, permutation)`](../../../algorithm/BasicAlgorithms.py#L377) | permutationに従ってvaluesを並べ替えた新しいlistを返す。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`permutation`: 0-indexed置換 `p[i]` の列 | list[object] — permutationの順にvaluesの要素を並べた新しいlist |
| [`permute_in_place(values, permutation)`](../../../algorithm/BasicAlgorithms.py#L383) | permutationに従ってvalues自体を破壊的に並べ替える。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`permutation`: 0-indexed置換 `p[i]` の列 | list[object] — permutationの順へ並べ替えた入力valuesと同じobject |
| [`bucket_sort_permutation(keys, maximum=None)`](../../../algorithm/BasicAlgorithms.py#L397) | 非負整数keyを安定sortしたときの元の添字順を返す。 | `keys`: 整列keyの列またはkey callback<br>`maximum`: 最大として使う入力。省略時: `None` | list[int] — 頂点または要素の位置を結果順に並べた列 / 計算結果（数値または入力要素型） |
| [`bucket_sort(values, key=lambda value: value, maximum=None)`](../../../algorithm/BasicAlgorithms.py#L417) | 非負整数keyを使ってvaluesを安定sortした列を返す。 | `values`: 初期値のiterable。整数ならsizeを表す場合がある<br>`key`: 比較・格納に使うkey。省略時: `lambda value: value`<br>`maximum`: 最大として使う入力。省略時: `None` | `permute(values, bucket_sort_permutation(keys, maximum))` |

## Class `Mo`

Fibonacci・転倒数・LIS・0/1 knapsack・subset sum・Mo・doublingを扱う `Mo`。

- constructor: [`Mo(n, query_count=0, block_size=None)`](../../../algorithm/BasicAlgorithms.py#L145)
- 引数: `n`: 要素数・頂点数・次数<br>`query_count`: 処理中に呼び出す関数または操作。省略時: `0`<br>`block_size`: 1ブロックに含める要素数。省略時: `None`
- 返り値: `Mo` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`add_query(left, right)`](../../../algorithm/BasicAlgorithms.py#L150) | method | 半開区間の問い合わせを登録し、そのquery IDを返す。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない） | 数値または入力要素型 `len(self.queries) - 1` |
| [`order()`](../../../algorithm/BasicAlgorithms.py#L156) | method | `order`を求める。 | なし | `sorted(self.queries, key=lambda query: (query[0] // width, que...` |
| [`run(add_left, add_right, remove_left, remove_right, get)`](../../../algorithm/BasicAlgorithms.py#L166) | method | 登録済みの処理を実行し、入力順に結果を返す。 | `add_left`: 処理中に呼び出す関数または操作<br>`add_right`: 処理中に呼び出す関数または操作<br>`remove_left`: 処理中に呼び出す関数または操作<br>`remove_right`: 処理中に呼び出す関数または操作<br>`get`: 処理中に呼び出す関数または操作 | 答え（数値または入力要素型） |

## Class `Doubling`

Fibonacci・転倒数・LIS・0/1 knapsack・subset sum・Mo・doublingを扱う `Doubling`。

- constructor: [`Doubling(successor, max_steps, values=None)`](../../../algorithm/BasicAlgorithms.py#L191)
- 引数: `successor`: `successor`として使う入力<br>`max_steps`: 最大・`steps`の上限<br>`values`: 初期値のiterable。整数ならsizeを表す場合がある。省略時: `None`
- 返り値: `Doubling` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`jump(vertex, steps)`](../../../algorithm/BasicAlgorithms.py#L203) | method | `jump`を求める。 | `vertex`: 頂点番号（0-indexed）<br>`steps`: 移動step数 | `vertex` |
| [`jump_with_sum(vertex, steps)`](../../../algorithm/BasicAlgorithms.py#L212) | method | `jump`・`with`・和を計算する。 | `vertex`: 頂点番号（0-indexed）<br>`steps`: 移動step数 | tuple(`vertex`, 合計値（int）) |

## Module aliases

- [`Popcount`](../../../algorithm/BasicAlgorithms.py#L423) = `popcount`
- [`MsbIndex`](../../../algorithm/BasicAlgorithms.py#L424) = `most_significant_bit_index`
- [`LsbIndex`](../../../algorithm/BasicAlgorithms.py#L425) = `least_significant_bit_index`
