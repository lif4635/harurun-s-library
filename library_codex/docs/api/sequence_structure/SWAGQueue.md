<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.sequence_structure.SWAGQueue`

queue全体のmonoid積をpush・popと同じ償却計算量で保つSWAG。

- source: [`sequence_structure/SWAGQueue.py`](../../../sequence_structure/SWAGQueue.py)
- 公開API: function 0、class 1、method/property 7（Python protocol 3を含む）

## できること

- `SWAGQueue`: queue全体のmonoid積をpush・popと同じ償却計算量で保つSWAGを扱う `SWAGQueue`。

## Import

```python
from library_codex.sequence_structure.SWAGQueue import SWAGQueue
```

## Class `SWAGQueue`

queue全体のmonoid積をpush・popと同じ償却計算量で保つSWAGを扱う `SWAGQueue`。

- constructor: [`SWAGQueue(op, identity)`](../../../sequence_structure/SWAGQueue.py#L6)
- 引数: `op`: 結合的な二項演算 `op(left, right)`<br>`identity`: 演算 `op` の単位元
- 返り値: `SWAGQueue` instance
- 計算量: —

| method / property | 種別 | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- | --- |
| [`append(value)`](../../../sequence_structure/SWAGQueue.py#L12) | method | 要素を追加する。 | `value`: 追加・設定・問い合わせる値 | `None` | 償却 O(1) 回のop呼び出し |
| [`popleft()`](../../../sequence_structure/SWAGQueue.py#L18) | method | `popleft`を求める。 | なし | `self.front.pop()[0]` | 償却 O(1) 回のop呼び出し |
| [`fold()`](../../../sequence_structure/SWAGQueue.py#L30) | method | 指定範囲・pathを演算で集約する。 | なし | `self.back[-1][1] if self.back else self.identity` / `self.front[-1][1]` / `self.op(self.front[-1][1], self.back[-1][1])` | O(1) 回のop呼び出し |
| [`__len__()`](../../../sequence_structure/SWAGQueue.py#L40) | method | len(obj)。 | なし | 要素数（int） | — |
| [`tolist()`](../../../sequence_structure/SWAGQueue.py#L43) | method | queueの先頭から順に要素を並べたlistを返す。O(N)。 | なし | list[object] — queueの先頭から末尾までの要素列 | O(N) |
| [`__str__()`](../../../sequence_structure/SWAGQueue.py#L49) | method | str(obj)・print(obj)で論理内容を表示する。 | なし | str instance | O(N) |
| [`__repr__()`](../../../sequence_structure/SWAGQueue.py#L52) | method | 対話環境・debugger向けに型名付きで表示する。 | なし | 数値または入力要素型 `'SWAGQueue(%r)' % self.tolist()` | O(N) |
