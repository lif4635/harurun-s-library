<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph.BiconnectedComponents`

二重頂点連結成分・block-cut forest（自己ループ・平行辺対応）。

- 計算量の目安: $O(V+E)$
- source: [`graph/BiconnectedComponents.py`](../../../graph/BiconnectedComponents.py)
- 公開API: function 0、class 2、method/property 6（Python protocol 2を含む）

## Import

```python
from library_codex.graph.BiconnectedComponents import BiconnectedComponents, BlockCutTree
```

## Class `BiconnectedComponents`

二重頂点連結成分・block-cut forest（自己ループ・平行辺対応）を扱う `BiconnectedComponents`。

- constructor: [`BiconnectedComponents(vertex_count, edges=None)`](../../../graph/BiconnectedComponents.py#L9)
- 引数: `vertex_count`: 頂点数<br>`edges`: 辺のiterable/list。省略時: `None`
- 返り値: `BiconnectedComponents` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`components`](../../../graph/BiconnectedComponents.py#L109) | property | `BiconnectedComponents` の `components method` を実行する。 | なし | `self.vertex_components` |
| [`bc`](../../../graph/BiconnectedComponents.py#L113) | property | `BiconnectedComponents` の `bc method` を実行する。 | なし | list |

## Class `BlockCutTree`

二重頂点連結成分・block-cut forest（自己ループ・平行辺対応）を扱う `BlockCutTree`。

- constructor: [`BlockCutTree(vertex_count, edges=None)`](../../../graph/BiconnectedComponents.py#L127)
- 引数: `vertex_count`: 頂点数<br>`edges`: 辺のiterable/list。省略時: `None`
- 返り値: `BlockCutTree` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`id(vertex)`](../../../graph/BiconnectedComponents.py#L163) | method | `BlockCutTree` の `id method` を実行する。 | `vertex`: 頂点番号（0-indexed） | `self.vertex_id[vertex]` |
| [`is_articulation(vertex)`](../../../graph/BiconnectedComponents.py#L166) | method | `articulation`かどうかを判定する。 | `vertex`: 頂点番号（0-indexed） | bool |
| [`__len__()`](../../../graph/BiconnectedComponents.py#L171) | method | len(obj)。 | なし | 要素数（int） |
| [`__getitem__(node)`](../../../graph/BiconnectedComponents.py#L174) | method | obj[key] で取得する。 | `node`: 頂点・内部node番号 | 格納値、sliceなら同種の部分構造 |
| [`is_arti`](../../../graph/BiconnectedComponents.py#L169) | alias | `is_articulation` の別名。 | 同じ | 同じ |

## Module aliases

- [`BiConnectedComponents`](../../../graph/BiconnectedComponents.py#L178) = `BiconnectedComponents`
