<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph.DAGAntichain`

DAGが表す半順序の最大反鎖を求める。

- source: [`graph/DAGAntichain.py`](../../../graph/DAGAntichain.py)
- 公開API: function 1、class 0、method/property 0（Python protocol 0を含む）

## できること

- DAGから、どちらから他方へも到達できない頂点を最大個数選べる。
- 利用側で推移閉包や比較可能性graphを作らず、通常のDAG隣接listをそのまま渡せる。

## Import

```python
from library_codex.graph.DAGAntichain import maximum_antichain
```

## Functions

| signature | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- |
| [`maximum_antichain(graph)`](../../../graph/DAGAntichain.py#L10) | DAGの到達関係を半順序とみなし、互いに比較不能な頂点からなる最大反鎖を1つ返す。 | `graph`: DAGの有向隣接list。要素は行き先、または先頭要素が行き先のtuple。辺u→vはuがvより前にある関係を表す。cycleがあればValueError。 | list[int] — 最大反鎖に選んだ頂点番号を昇順に並べた列。任意の異なる2頂点u, vについて、uからvにもvからuにも到達できない。 | O((V + E) ceil(V / W) + R sqrt(V)) time、O(V ceil(V / W) + R) memory（Rは到達可能な異なる頂点pair数、Wはbitsetの機械語幅） |
