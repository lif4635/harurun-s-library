<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph.ExpandedGraph`

Range Edge Graph・多次元grid・reverse/grid変換。

- 計算量の目安: 区間辺 $O(\log N)$ 補助辺
- source: [`graph/ExpandedGraph.py`](../../../graph/ExpandedGraph.py)
- 公開API: function 2、class 2、method/property 15（Python protocol 2を含む）

## できること

- `reverse_graph`: `reverse`・グラフを求める。
- `grid_to_graph`: `grid`・`to`・グラフを求める。
- `RangeEdgeGraph`: Range Edge Graph・多次元grid・reverse/grid変換を扱う `RangeEdgeGraph`。
- `DimensionExpandedGraph`: Range Edge Graph・多次元grid・reverse/grid変換を扱う `DimensionExpandedGraph`。

## Import

```python
from library_codex.graph.ExpandedGraph import reverse_graph, grid_to_graph, RangeEdgeGraph, DimensionExpandedGraph
```

## Functions

| signature | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- |
| [`reverse_graph(graph)`](../../../graph/ExpandedGraph.py#L272) | `reverse`・グラフを求める。 | `graph`: 隣接listまたはグラフobject | list[object] — 用途欄に示した結果を1要素ずつ並べた列 |
| [`grid_to_graph(grid, passable=lambda value: value != '#')`](../../../graph/ExpandedGraph.py#L284) | `grid`・`to`・グラフを求める。 | `grid`: `grid`として使う入力<br>`passable`: `passable`として使う入力。省略時: `lambda value: value != '#'` | tuple(`graph`（list）, `lambda row, column: row * width + column`, `lambda vertex: divmod(vertex, width)`) |

## Class `RangeEdgeGraph`

Range Edge Graph・多次元grid・reverse/grid変換を扱う `RangeEdgeGraph`。

- constructor: [`RangeEdgeGraph(n, zero=0)`](../../../graph/ExpandedGraph.py#L20)
- 引数: `n`: 要素数・頂点数・次数<br>`zero`: 加法単位元・0相当の値。省略時: `0`
- 返り値: `RangeEdgeGraph` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`__len__()`](../../../graph/ExpandedGraph.py#L85) | method | len(obj)。 | なし | 要素数（int） |
| [`add_point_to_point(source, target, cost)`](../../../graph/ExpandedGraph.py#L119) | method | `point`・`to`・`point`を追加する。 | `source`: 始点<br>`target`: 探索・判定・更新の対象値<br>`cost`: 辺・選択の費用 | `None` |
| [`add_point_to_range(source, left, right, cost)`](../../../graph/ExpandedGraph.py#L124) | method | `point`・`to`・区間を追加する。 | `source`: 始点<br>`left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`cost`: 辺・選択の費用 | `None` |
| [`add_range_to_point(left, right, target, cost)`](../../../graph/ExpandedGraph.py#L130) | method | 区間・`to`・`point`を追加する。 | `left`: 半開区間の左端（含む）<br>`right`: 半開区間の右端（含まない）<br>`target`: 探索・判定・更新の対象値<br>`cost`: 辺・選択の費用 | `None` |
| [`add_range_to_range(from_left, from_right, to_left, to_right, cost)`](../../../graph/ExpandedGraph.py#L136) | method | 区間・`to`・区間を追加する。 | `from_left`: `from`・左として使う入力<br>`from_right`: `from`・右として使う入力<br>`to_left`: `to`・左として使う入力<br>`to_right`: `to`・右として使う入力<br>`cost`: 辺・選択の費用 | `None` |
| [`add_edge(source, target, cost)`](../../../graph/ExpandedGraph.py#L150) | method | 辺を追加する。 | `source`: 始点<br>`target`: 探索・判定・更新の対象値<br>`cost`: 辺・選択の費用 | `None` |

## Class `DimensionExpandedGraph`

Range Edge Graph・多次元grid・reverse/grid変換を扱う `DimensionExpandedGraph`。

- constructor: [`DimensionExpandedGraph(*shape, extra=0)`](../../../graph/ExpandedGraph.py#L159)
- 引数: `*shape`: `shape`として使う入力<br>`extra`: `extra`として使う入力。省略時: `0`
- 返り値: `DimensionExpandedGraph` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`__len__()`](../../../graph/ExpandedGraph.py#L171) | method | len(obj)。 | なし | 要素数（int） |
| [`valid(coordinate)`](../../../graph/ExpandedGraph.py#L174) | method | `valid`を求める。 | `coordinate`: 多次元座標 | bool |
| [`id(coordinate)`](../../../graph/ExpandedGraph.py#L181) | method | `id`を求める。 | `coordinate`: 多次元座標 | `sum((value * stride for (value, stride) in zip(coordinate, sel...` |
| [`coordinate(vertex)`](../../../graph/ExpandedGraph.py#L187) | method | `coordinate`を求める。 | `vertex`: 頂点番号（0-indexed） | tuple — 用途欄に示した複数の結果を順に格納 |
| [`extra_id(index)`](../../../graph/ExpandedGraph.py#L195) | method | `extra`・`id`を求める。 | `index`: 0-indexedの位置 | 数値または入力要素型 `self.grid_size + index` |
| [`neighbors(coordinate)`](../../../graph/ExpandedGraph.py#L200) | method | 指定頂点から出る辺を (行き先, 重み, 辺ID) の順で列挙する。 | `coordinate`: 多次元座標 | iterator[tuple[int, number, int]] — (行き先頂点, 辺重み, 元の辺ID)を辺ごとにyieldする |
| [`bfs(start, transitions=None)`](../../../graph/ExpandedGraph.py#L216) | method | `bfs`を求める。 | `start`: 始点・開始位置<br>`transitions`: 遷移・隣接移動のiterable。省略時: `None` | 距離（数値または入力要素型） |
| [`bfs01(start, transitions)`](../../../graph/ExpandedGraph.py#L235) | method | `bfs01`を求める。 | `start`: 始点・開始位置<br>`transitions`: 遷移・隣接移動のiterable | 距離（数値または入力要素型） |
| [`dijkstra(start, transitions)`](../../../graph/ExpandedGraph.py#L254) | method | `dijkstra`を求める。 | `start`: 始点・開始位置<br>`transitions`: 遷移・隣接移動のiterable | 距離（数値または入力要素型） |
