<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph.GraphOptimization`

Hungarian・Steiner木・Held--Karp TSP・Dial Dijkstra・有向最小全域木。

- 計算量の目安: 各標準計算量
- source: [`graph/GraphOptimization.py`](../../../graph/GraphOptimization.py)
- 公開API: function 8、class 0、method/property 0（Python protocol 0を含む）

## Import

```python
from library_codex.graph.GraphOptimization import (
    hungarian,
    hungarian_max,
    minimum_steiner_tree,
    steiner_tree_dp,
    held_karp_path,
    held_karp_cycle,
    dial_dijkstra,
    minimum_cost_arborescence,
)
```

## Functions

| signature | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- |
| [`hungarian(cost)`](../../../graph/GraphOptimization.py#L6) | Minimum-cost injection from rows to columns. Returns ``(minimum_cost, assignment)`` where ``assignment[i]`` is the column matched to row ``i``. Requires ``rows <= columns`` and supports negative integer or floating-po... | `cost`: 辺・選択の費用 | tuple(`0`, list `[]`) / tuple(`-v[0]`, `assignment`（数値または入力要素型）) |
| [`hungarian_max(cost)`](../../../graph/GraphOptimization.py#L66) | Maximum-cost injection from rows to columns. | `cost`: 辺・選択の費用 | tuple(`-value`, `assignment`) |
| [`minimum_steiner_tree(n, edges, terminals)`](../../../graph/GraphOptimization.py#L72) | Return ``(cost, edge_ids)`` of a minimum undirected Steiner tree. Edges are ``(u, v, nonnegative_weight)``. Complexity is O(3^K N + 2^K (N+M) log N), where K is the number of distinct terminals. If the terminals canno... | `n`: 要素数・頂点数・次数<br>`edges`: 辺のiterable/list<br>`terminals`: Steiner木で接続するterminal頂点列 | ``(cost, edge_ids)`` of a minimum undirected Steiner tree |
| [`steiner_tree_dp(n, edges, terminals)`](../../../graph/GraphOptimization.py#L149) | Return only the standard subset DP table for Steiner tree costs. | `n`: 要素数・頂点数・次数<br>`edges`: 辺のiterable/list<br>`terminals`: Steiner木で接続するterminal頂点列 | only the standard subset DP table for Steiner tree costs |
| [`held_karp_path(distance, start=None, goal=None, restore=False)`](../../../graph/GraphOptimization.py#L191) | Minimum Hamiltonian path cost, optionally fixing either endpoint. ``distance[u][v]`` is the direct transition cost; no metric closure is performed. Returns the cost, or ``(cost, vertex_order)`` with ``restore=True``. ... | `distance`: 距離・距離配列<br>`start`: 始点・開始位置。省略時: `None`<br>`goal`: 終点。Noneなら全体を処理。省略時: `None`<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/int `(0, []) if restore else 0` / 答え / tuple(`inf`（float）, list `[]`) / tuple(答え, `order`（list）) |
| [`held_karp_cycle(distance, start=0, restore=False)`](../../../graph/GraphOptimization.py#L255) | Minimum Hamiltonian cycle through ``start``. | `distance`: 距離・距離配列<br>`start`: 始点・開始位置。省略時: `0`<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/int `(result, [start, start] if n else []) if restore else result` / 答え（数値または入力要素型） / tuple(`inf`（float）, list `[]`) / tuple(答え（数値または入力要素型）, `order`（list）) |
| [`dial_dijkstra(graph, starts, max_weight, restore=False)`](../../../graph/GraphOptimization.py#L311) | Dijkstra with circular buckets for integer weights in [0,max_weight]. | `graph`: 隣接listまたはグラフobject<br>`starts`: `starts`として渡す値（APIの文脈に従う）<br>`max_weight`: 最大・`weight`の上限<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/数値または入力要素型 `(distance, parent) if restore else distance` |
| [`minimum_cost_arborescence(n, root, edges)`](../../../graph/GraphOptimization.py#L351) | Return a minimum directed spanning arborescence rooted at ``root``. Edges are ``(source, target, cost)`` and may have negative costs or be parallel. The result is ``(cost, original_edge_ids)``; ``None`` means some ver... | `n`: 要素数・頂点数・次数<br>`root`: 根の頂点番号・原始根<br>`edges`: 辺のiterable/list | a minimum directed spanning arborescence rooted at ``root`` / `None` |
