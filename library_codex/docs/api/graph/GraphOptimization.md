<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph.GraphOptimization`

Hungarian・Steiner木・Held--Karp TSP・Dial Dijkstra・有向最小全域木。

- 計算量の目安: 各標準計算量
- source: [`graph/GraphOptimization.py`](../../../graph/GraphOptimization.py)
- 公開API: function 8、class 0、method/property 0（Python protocol 0を含む）

## できること

- `hungarian`: `hungarian`を求める。
- `hungarian_max`: `hungarian`・最大を求める。
- `minimum_steiner_tree`: 最小・`steiner`・木を求める。
- `steiner_tree_dp`: `steiner`・木・`dp`を求める。

## Import

```python
from library_codex.graph.GraphOptimization import (
    hungarian,
    hungarian_max,
    minimum_steiner_tree,
    steiner_tree_dp,
    held_karp_path,
    held_karp_cycle,
    dial_dijkstra,
    minimum_cost_arborescence,
)
```

## Functions

| signature | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- |
| [`hungarian(cost)`](../../../graph/GraphOptimization.py#L6) | `hungarian`を求める。 | `cost`: 辺・選択の費用 | tuple(`0`, list `[]`) / tuple(`-v[0]`, 各変数へ割り当てる0/1のlist（数値または入力要素型）) |
| [`hungarian_max(cost)`](../../../graph/GraphOptimization.py#L66) | `hungarian`・最大を求める。 | `cost`: 辺・選択の費用 | tuple(`-value`, 各変数へ割り当てる0/1のlist) |
| [`minimum_steiner_tree(n, edges, terminals)`](../../../graph/GraphOptimization.py#L72) | 最小・`steiner`・木を求める。 | `n`: 要素数・頂点数・次数<br>`edges`: 辺のiterable/list<br>`terminals`: Steiner木で接続するterminal頂点列 | tuple(`0`, list `[]`) / tuple(`inf`（float）, list `[]`) / tuple(答え, list `list(selected)`) |
| [`steiner_tree_dp(n, edges, terminals)`](../../../graph/GraphOptimization.py#L149) | `steiner`・木・`dp`を求める。 | `n`: 要素数・頂点数・次数<br>`edges`: 辺のiterable/list<br>`terminals`: Steiner木で接続するterminal頂点列 | list[object] — 用途欄に示した結果を1要素ずつ並べた列 |
| [`held_karp_path(distance, start=None, goal=None, restore=False)`](../../../graph/GraphOptimization.py#L191) | `held`・`karp`・pathを求める。 | `distance`: 距離・距離配列<br>`start`: 始点・開始位置。省略時: `None`<br>`goal`: 終点。Noneなら全体を処理。省略時: `None`<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/int `(0, []) if restore else 0` / 答え / tuple(`inf`（float）, list `[]`) / tuple(答え, 頂点・要素を処理順に並べたlist[int]) |
| [`held_karp_cycle(distance, start=0, restore=False)`](../../../graph/GraphOptimization.py#L255) | `held`・`karp`・閉路を求める。 | `distance`: 距離・距離配列<br>`start`: 始点・開始位置。省略時: `0`<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/int `(result, [start, start] if n else []) if restore else result` / 答え（数値または入力要素型） / tuple(`inf`（float）, list `[]`) / tuple(答え（数値または入力要素型）, 頂点・要素を処理順に並べたlist[int]) |
| [`dial_dijkstra(graph, starts, max_weight, restore=False)`](../../../graph/GraphOptimization.py#L311) | `dial`・`dijkstra`を求める。 | `graph`: 隣接listまたはグラフobject<br>`starts`: `starts`として使う入力<br>`max_weight`: 最大・`weight`の上限<br>`restore`: 復元情報も計算するか。省略時: `False` | tuple/数値または入力要素型 `(distance, parent) if restore else distance` |
| [`minimum_cost_arborescence(n, root, edges)`](../../../graph/GraphOptimization.py#L351) | 最小・`cost`・`arborescence`を求める。 | `n`: 要素数・頂点数・次数<br>`root`: 根の頂点番号・原始根<br>`edges`: 辺のiterable/list | tuple(`0`, list `[]`) / tuple(`sum((edges[edge_id][2] for edge_id in original_ids))`, `original_ids`（list）) / `None` |
