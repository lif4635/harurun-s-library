<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.string_sequence.LongestCommonSubstring`

2列の最長共通substringをsuffix automatonで求める。

- source: [`string_sequence/LongestCommonSubstring.py`](../../../string_sequence/LongestCommonSubstring.py)
- 公開API: function 1、class 0、method/property 0（Python protocol 0を含む）

## できること

- 2列に連続して現れる最長の共通部分列について、長さと両方の出現区間を求められる。
- firstのsuffix automatonを構築し、列の切り出しや候補substringの列挙を行わない。

## Import

```python
from library_codex.string_sequence.LongestCommonSubstring import longest_common_substring
```

## Functions

| signature | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- |
| [`longest_common_substring(first, second)`](../../../string_sequence/LongestCommonSubstring.py#L4) | firstとsecondの両方に連続して現れる最長列をsuffix automatonで求める。 | `first`: hash可能な要素を並べた1つ目の列。<br>`second`: 同じ種類の要素を並べた2つ目の列。 | tuple[int, tuple[int, int], tuple[int, int]] — (length, first_range, second_range)。各rangeは一致するsubstringの半開区間で、長さはlength。同じ最長substringが複数ある場合はいずれか1つを返す。共通要素がなければ(0, (0, 0), (0, 0))。 | 期待 O(N+M) time、O(N) memory（hash table操作を期待O(1)とする） |
