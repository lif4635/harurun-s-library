<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph_matching.GeneralMatching`

一般無向グラフの最大マッチングを求める。

- source: [`graph_matching/GeneralMatching.py`](../../../graph_matching/GeneralMatching.py)
- 公開API: function 0、class 1、method/property 2（Python protocol 0を含む）

## できること

- 奇数cycleを含む一般無向グラフの最大cardinality matchingをblossom縮約で求められる。
- 各頂点のmateとマッチした辺の列を取得できる。
- すべての最大マッチングで必ずマッチされる頂点を判定できる。

## Import

```python
from library_codex.graph_matching.GeneralMatching import GeneralMatching
```

## Class `GeneralMatching`

奇数cycleを含む一般無向グラフの最大cardinality matchingをblossom縮約で求める。

- constructor: [`GeneralMatching(graph)`](../../../graph_matching/GeneralMatching.py#L10)
- 引数: `graph`: 0始まりの頂点番号を持つ無向グラフの隣接list。各辺は両端に入れる。
- 返り値: `GeneralMatching` instance
- 計算量: O(VE)
- 作成後: graphを直ちに解き、matching_size・mate・pairsを取得できる状態を作る。

| method / property | 種別 | 用途 | 引数 | 返り値 | 計算量 |
| --- | --- | --- | --- | --- | --- |
| [`pairs()`](../../../graph_matching/GeneralMatching.py#L117) | method | 構築時に得た最大マッチングの辺を重複なく列挙する。 | なし | list[tuple[int, int]] — 各要素はマッチした無向辺(first, second)。常にfirst < second。 | O(V) |
| [`essential_vertices()`](../../../graph_matching/GeneralMatching.py#L121) | method | すべての最大マッチングで必ずマッチされる頂点を判定する。 | なし | list[bool] — 頂点番号順の真偽値列。結果[v]は、頂点vがどの最大マッチングでも必ず何らかの辺とマッチされる場合だけTrue。 | O(VE) |
