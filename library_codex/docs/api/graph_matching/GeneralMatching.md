<!-- Generated by tools/build_api_reference.py; do not edit directly. -->
# `library_codex.graph_matching.GeneralMatching`

一般グラフの最大matchingを求める。

- 計算量の目安: 各操作の計算量はAPI表を参照
- source: [`graph_matching/GeneralMatching.py`](../../../graph_matching/GeneralMatching.py)
- 公開API: function 1、class 1、method/property 1（Python protocol 0を含む）

## できること

- `maximum_general_matching`: 最大・`general`・matchingを求める。
- `GeneralMatching`: 一般グラフの最大matchingを求めるを扱う `GeneralMatching`。

## Import

```python
from library_codex.graph_matching.GeneralMatching import maximum_general_matching, GeneralMatching
```

## Functions

| signature | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- |
| [`maximum_general_matching(graph)`](../../../graph_matching/GeneralMatching.py#L103) | 最大・`general`・matchingを求める。 | `graph`: 隣接listまたはグラフobject | tuple(`solver.matching_size`, `solver.mate`) |

## Class `GeneralMatching`

一般グラフの最大matchingを求めるを扱う `GeneralMatching`。

- constructor: [`GeneralMatching(graph)`](../../../graph_matching/GeneralMatching.py#L8)
- 引数: `graph`: 隣接listまたはグラフobject
- 返り値: `GeneralMatching` instance

| method / property | 種別 | 用途 | 引数 | 返り値 |
| --- | --- | --- | --- | --- |
| [`pairs()`](../../../graph_matching/GeneralMatching.py#L98) | method | matchingに含まれる左右頂点の組を列挙する。 | なし | list[object] — 用途欄に示した結果を1要素ずつ並べた列 |
